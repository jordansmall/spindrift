- Pull any parent/linked issue or PRD it references too via Forgejo — the
  subject issue's own body and its last-10-comment snapshot are already in
  the # ISSUE TEXT section below.
- `git log -n 10 --oneline` — recent history.
