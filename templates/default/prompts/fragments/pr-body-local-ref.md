3. Body MUST contain `Local-issue: ${ISSUE_NUMBER}` — a plain breadcrumb,
   never an auto-closing reference: a local ticket's slug can be numeric, and
   an auto-close keyword would silently close an unrelated GitHub issue on
   the Target repo. Summarize what changed and flag anything a reviewer
   should know.
