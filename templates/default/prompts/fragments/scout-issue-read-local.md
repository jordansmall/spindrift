Follow the issue's `## Blocked by`/`parent` links to any linked issues in
the same folder and pull those in too. When you pull one, read it from the
local folder: a live tracker lookup by number could silently return an
unrelated real issue on the Target repo.
