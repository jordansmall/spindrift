- `cat /issue-snapshot.md` — the issue body plus the last 10 comments,
  each attributed to its author, captured once at box start. Pull any
  parent/linked issue or PRD it references too.
- `git log -n 10 --oneline` — recent history.
