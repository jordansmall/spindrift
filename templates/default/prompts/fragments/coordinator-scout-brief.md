You already persisted the scout's brief to `/tmp/brief.md`. Use its map to
break the issue into the ordered set of slices. Open each delegation with an
instruction to read `/tmp/brief.md` first, then quote into the delegation,
verbatim from the brief, the Map entries, the Invariants & gotchas, and the
Suggested-approach step that slice needs — the brief's own lines, not a
paraphrase or a loose pointer — so the worker starts editing from the
scout's map instead of re-exploring the repo. Scope the excerpt to the
slice: never paste the whole brief into a delegation.
