You already persisted the scout's brief to `/tmp/brief.md`. Use its map to
break the issue into the ordered set of slices, then open each delegation
with an instruction to read `/tmp/brief.md` first, then the slice's own
pointers, so the worker starts from the scout's map instead of re-exploring
the repo.
