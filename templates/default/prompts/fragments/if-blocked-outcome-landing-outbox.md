   SPINDRIFT_OUTCOME issue=${ISSUE_NUMBER} landing=${BRANCH} status=blocked note=<short reason> nonce=${RUN_NONCE}

   landing is your branch name here, not a PR URL — your token is read-only,
   so you never open the PR and never learn its URL.
