# path-form skill fixture (issue #2114)
#
# Intentionally minimal: no skill frontmatter/name. This file is never
# built or installed — the skills-path-form-drvpath-host-independent check
# only points the `skills` knob at this directory as a plain source path to
# probe that a content-addressed path form leaves the image drvPath
# host-independent. Its contents are irrelevant beyond existing.
